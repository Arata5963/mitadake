# Active Storageファイル保存設定
# ※本プロジェクトはCarrierWave+S3を使用（Active Storage未使用）

test:
  service: Disk
  root: <%= Rails.root.join("tmp/storage") %>

local:
  service: Disk
  root: <%= Rails.root.join("storage") %>
