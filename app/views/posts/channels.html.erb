<%# app/views/posts/channels.html.erb %>
<%# チャンネル一覧ページ（YouTube Music風リスト表示） %>

<div class="min-h-screen bg-white">
  <div class="max-w-4xl mx-auto px-4 sm:px-8 py-6">
    <%# ヘッダー %>
    <div class="flex items-center gap-4 mb-6">
      <%= link_to posts_path, class: "text-gray-400 hover:text-gray-600 transition-colors" do %>
        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"/>
        </svg>
      <% end %>
      <h1 class="text-xl font-bold text-gray-900">人気のチャンネル</h1>
      <span class="text-sm text-gray-500"><%= @channels.size %>チャンネル</span>
    </div>

    <% if @channels.present? %>
      <div class="space-y-3">
        <% @channels.each_with_index do |channel, index| %>
          <% rank = index + 1 %>
          <%= link_to posts_path(channel: channel[:channel_name]), class: "flex items-center gap-4 group hover:bg-gray-50 rounded-lg p-2 -mx-2 transition-colors" do %>
            <%# ランキング番号 %>
            <div class="flex-shrink-0 w-10 text-center">
              <span class="text-2xl font-black <%= rank <= 3 ? 'text-orange-500' : 'text-gray-300' %>">
                <%= rank %>
              </span>
            </div>

            <%# チャンネルアイコン %>
            <div class="flex-shrink-0 w-14 h-14 rounded-full overflow-hidden bg-gray-100 ring-2 ring-transparent group-hover:ring-orange-300 transition-all">
              <% if channel[:thumbnail_url].present? %>
                <%= image_tag channel[:thumbnail_url],
                      alt: channel[:channel_name],
                      class: "w-full h-full object-cover",
                      loading: "lazy" %>
              <% else %>
                <div class="w-full h-full flex items-center justify-center bg-gray-200">
                  <svg class="w-6 h-6 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"/>
                  </svg>
                </div>
              <% end %>
            </div>

            <%# チャンネル名 %>
            <div class="flex-1 min-w-0">
              <h3 class="text-sm font-medium text-gray-900 group-hover:text-orange-600 transition-colors truncate">
                <%= channel[:channel_name] %>
              </h3>
              <p class="text-xs text-gray-500 mt-0.5">
                <%= channel[:post_count] %>本の動画
              </p>
            </div>

            <%# アクション数 %>
            <div class="flex-shrink-0 text-right">
              <span class="text-sm font-bold text-orange-500"><%= channel[:action_count] %></span>
              <span class="text-xs text-gray-400">件</span>
            </div>
          <% end %>
        <% end %>
      </div>
    <% else %>
      <div class="py-16 text-center">
        <span class="text-5xl block mb-4">📺</span>
        <p class="text-gray-700 font-medium">チャンネルがまだありません</p>
      </div>
    <% end %>
  </div>
</div>
