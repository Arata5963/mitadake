<%# app/views/users/designs/_mypage_story.html.erb %>
<%# デザイン: ストーリー風（Instagram/TikTok風） %>

<% current_action = user.current_action_plan %>
<% current_video = user.current_video %>
<% achieved_count = user.post_entries.achieved.count %>

<div class="max-w-sm mx-auto px-4 py-6">
  <%# プロフィールセクション %>
  <div class="flex items-start gap-5 mb-6">
    <%# アバター（リング付き） %>
    <div class="flex-shrink-0">
      <div class="relative">
        <div class="<%= current_action ? 'p-0.5 bg-gradient-to-tr from-yellow-400 via-red-500 to-purple-600' : 'p-0.5 bg-gray-200' %> rounded-full">
          <div class="p-0.5 bg-white rounded-full">
            <% if user.avatar.present? %>
              <%= image_tag user.avatar.url, class: "w-20 h-20 rounded-full object-cover" %>
            <% else %>
              <div class="w-20 h-20 rounded-full bg-gray-100 flex items-center justify-center">
                <span class="text-gray-400 text-2xl font-bold"><%= user.name&.first&.upcase || '?' %></span>
              </div>
            <% end %>
          </div>
        </div>
        <% if current_action %>
          <div class="absolute -bottom-0.5 left-1/2 -translate-x-1/2 px-1.5 py-0.5 bg-gradient-to-r from-yellow-400 via-red-500 to-purple-600 rounded text-[10px] font-bold text-white uppercase tracking-wider">
            Live
          </div>
        <% end %>
      </div>
    </div>

    <%# 統計 %>
    <div class="flex-1 pt-2">
      <div class="flex gap-4">
        <div class="text-center">
          <p class="text-lg font-bold text-gray-900"><%= achieved_count %></p>
          <p class="text-xs text-gray-500">達成</p>
        </div>
        <div class="text-center">
          <p class="text-lg font-bold text-gray-900"><%= user.posts.count %></p>
          <p class="text-xs text-gray-500">投稿</p>
        </div>
        <div class="text-center">
          <p class="text-lg font-bold text-gray-900"><%= user.post_entries.count %></p>
          <p class="text-xs text-gray-500">プラン</p>
        </div>
      </div>
    </div>
  </div>

  <%# 名前とプロフィール編集 %>
  <div class="mb-4">
    <div class="flex items-center gap-2">
      <h1 class="font-bold text-gray-900"><%= user.name.presence || "名無しさん" %></h1>
      <%= link_to edit_profile_path, class: "text-gray-400 hover:text-gray-600" do %>
        <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
          <path stroke-linecap="round" stroke-linejoin="round" d="M15.232 5.232l3.536 3.536m-2.036-5.036a2.5 2.5 0 113.536 3.536L6.5 21.036H3v-3.572L16.732 3.732z" />
        </svg>
      <% end %>
    </div>
    <p class="text-sm text-gray-500">動画で学んで、行動に変える</p>
  </div>

  <% if current_video && current_action %>
    <%# 現在のフォーカス %>
    <div class="bg-gradient-to-br from-gray-50 to-gray-100 rounded-3xl p-4 mb-4" data-controller="hero-video" data-hero-video-video-id-value="<%= current_video.youtube_video_id %>">
      <div class="flex items-center gap-2 mb-3">
        <div class="flex items-center gap-1.5 px-2 py-1 bg-white rounded-full shadow-sm">
          <div class="w-1.5 h-1.5 rounded-full bg-red-500 animate-pulse"></div>
          <span class="text-xs font-medium text-gray-700">NOW</span>
        </div>
      </div>

      <%# 動画 %>
      <div class="group relative cursor-pointer rounded-2xl overflow-hidden shadow-lg" style="aspect-ratio: 16/9;">
        <iframe
          id="hero-player-<%= current_video.youtube_video_id %>"
          data-hero-video-target="iframe"
          src="https://www.youtube.com/embed/<%= current_video.youtube_video_id %>?autoplay=1&mute=1&loop=1&playlist=<%= current_video.youtube_video_id %>&controls=0&playsinline=1&rel=0&modestbranding=1&showinfo=0&enablejsapi=1&iv_load_policy=3"
          class="absolute inset-0 w-full h-full pointer-events-none"
          frameborder="0"
          allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
          allowfullscreen>
        </iframe>
        <div class="absolute inset-0 z-10 flex items-center justify-center opacity-0 group-hover:opacity-100 transition-opacity duration-200"
             data-action="click->hero-video#toggle">
          <div class="absolute inset-0 bg-black/30"></div>
          <button type="button" class="relative z-10 w-14 h-14 rounded-full bg-white/90 hover:bg-white flex items-center justify-center shadow-xl">
            <svg data-hero-video-target="playButton" class="w-6 h-6 text-black ml-0.5 hidden" viewBox="0 0 24 24" fill="currentColor">
              <path d="M8 5v14l11-7z"/>
            </svg>
            <svg data-hero-video-target="pauseButton" class="w-6 h-6 text-black" viewBox="0 0 24 24" fill="currentColor">
              <path d="M6 19h4V5H6v14zm8-14v14h4V5h-4z"/>
            </svg>
          </button>
        </div>
      </div>

      <%# 動画情報 %>
      <div class="mt-3">
        <%= link_to post_path(current_video), class: "block group/info" do %>
          <h3 class="text-sm font-semibold text-gray-900 line-clamp-1 group-hover/info:text-gray-600 transition-colors">
            <%= current_video.youtube_title %>
          </h3>
          <% if current_video.youtube_channel_name.present? %>
            <div class="flex items-center gap-1.5 mt-1">
              <% if current_video.youtube_channel_thumbnail_url.present? %>
                <%= image_tag current_video.youtube_channel_thumbnail_url, class: "w-4 h-4 rounded-full", alt: current_video.youtube_channel_name %>
              <% end %>
              <span class="text-gray-500 text-xs"><%= current_video.youtube_channel_name %></span>
            </div>
          <% end %>
        <% end %>
      </div>
    </div>

    <%# アクションカード %>
    <div class="bg-white border border-gray-200 rounded-2xl p-4">
      <div class="flex items-start gap-3">
        <%= button_to achieve_post_post_entry_path(current_video, current_action),
              method: :patch, class: "flex-shrink-0",
              form: { data: { turbo_confirm: "達成しますか？", turbo_frame: "_top" } } do %>
          <div class="w-6 h-6 rounded-full border-2 border-gray-300 hover:border-green-500 hover:bg-green-50 transition-all flex items-center justify-center group">
            <svg class="w-3 h-3 text-transparent group-hover:text-green-500 transition-colors" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="3">
              <path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7" />
            </svg>
          </div>
        <% end %>
        <div class="flex-1 min-w-0">
          <p class="text-gray-900 leading-relaxed"><%= current_action.content %></p>
          <% if current_action.deadline.present? %>
            <% is_overdue = current_action.deadline < Date.current %>
            <% is_today = current_action.deadline == Date.current %>
            <p class="text-xs mt-2 <%= is_overdue ? 'text-red-500 font-medium' : (is_today ? 'text-orange-500 font-medium' : 'text-gray-400') %>">
              <% if is_overdue %>
                ⚠️ 期限切れ
              <% elsif is_today %>
                🔥 今日まで
              <% else %>
                📅 <%= l(current_action.deadline, format: :short) %>まで
              <% end %>
            </p>
          <% end %>
        </div>
        <%= button_to post_post_entry_path(current_video, current_action),
              method: :delete,
              form: { data: { turbo_confirm: "削除しますか？", turbo_frame: "_top" } },
              class: "flex-shrink-0 text-gray-300 hover:text-red-500 transition-colors" do %>
          <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5">
            <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12" />
          </svg>
        <% end %>
      </div>
    </div>

  <% else %>
    <%# Empty State %>
    <div class="relative bg-gradient-to-br from-gray-50 to-gray-100 rounded-3xl p-8 text-center overflow-hidden">
      <div class="absolute top-0 right-0 w-32 h-32 bg-gradient-to-br from-purple-200/30 to-pink-200/30 rounded-full blur-2xl"></div>
      <div class="absolute bottom-0 left-0 w-24 h-24 bg-gradient-to-tr from-blue-200/30 to-cyan-200/30 rounded-full blur-2xl"></div>

      <div class="relative z-10">
        <div class="w-16 h-16 mx-auto mb-4 rounded-full bg-white shadow-lg flex items-center justify-center">
          <svg class="w-8 h-8 text-gray-400" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5">
            <path stroke-linecap="round" d="M15.75 10.5l4.72-4.72a.75.75 0 011.28.53v11.38a.75.75 0 01-1.28.53l-4.72-4.72M4.5 18.75h9a2.25 2.25 0 002.25-2.25v-9a2.25 2.25 0 00-2.25-2.25h-9A2.25 2.25 0 002.25 7.5v9a2.25 2.25 0 002.25 2.25z" />
          </svg>
        </div>
        <h2 class="text-lg font-bold text-gray-900 mb-1">始めよう</h2>
        <p class="text-sm text-gray-500 mb-5">動画を見て、今日やることを<br>決めてみませんか？</p>
        <%= link_to posts_path, class: "inline-flex items-center gap-2 px-5 py-2.5 bg-gray-900 text-white rounded-full font-medium hover:bg-gray-800 transition-colors" do %>
          動画を探す
          <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
            <path stroke-linecap="round" stroke-linejoin="round" d="M13 7l5 5m0 0l-5 5m5-5H6" />
          </svg>
        <% end %>
      </div>
    </div>
  <% end %>
</div>
