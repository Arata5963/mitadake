<%#
  app/views/devise/passwords/edit.html.erb
  ==========================================
  新しいパスワード設定ページ
  ==========================================

  【このページの役割】
  パスワードリセットメールのリンクからアクセスするページ。
  新しいパスワードを入力して変更を確定する。

  【アクセス方法】
  1. ログインページで「パスワードをお忘れの方」をクリック
  2. メールアドレスを入力してリセットメール送信
  3. メール内のリンクをクリック → このページ

  【フォーム送信先】
  PUT /users/password → Devise::PasswordsController#update

  【使用されるパーシャル】
  - devise/shared/_error_messages: エラー表示
  - shared/_password_field: パスワード入力フィールド（表示切替付き）

  【hidden_field :reset_password_token】
  メールのリンクに含まれるトークンをフォームに埋め込む。
  このトークンで正当なリセット要求か確認する。
%>
<% content_for :title, "新しいパスワードを設定" %>

<div class="min-h-screen bg-white">
  <div class="max-w-sm mx-auto px-4 py-16">

    <h1 class="text-xl font-bold text-gray-900 mb-2">新しいパスワードを設定</h1>
    <p class="text-sm text-gray-500 mb-8">
      安全なパスワードを設定してください
    </p>

    <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :put, class: "space-y-6", data: { turbo: false } }) do |f| %>
      <%= render "devise/shared/error_messages", resource: resource %>
      <%= f.hidden_field :reset_password_token %>

      <%= render 'shared/password_field',
            form: f,
            field_name: :password,
            label_text: "新しいパスワード",
            placeholder: "新しいパスワードを入力",
            autocomplete: "new-password",
            show_hint: true,
            minimum_length: 6 %>

      <%= render 'shared/password_field',
            form: f,
            field_name: :password_confirmation,
            label_text: "新しいパスワード（確認用）",
            placeholder: "新しいパスワードを再入力",
            autocomplete: "new-password" %>

      <div class="pt-4">
        <%= f.submit "パスワードを変更",
            class: "w-full bg-gray-900 text-white py-3 text-sm font-medium hover:bg-gray-800 transition-colors cursor-pointer" %>
      </div>
    <% end %>

    <div class="mt-8 pt-8 border-t border-gray-100">
      <%= link_to "← ログイン画面に戻る", new_user_session_path,
            class: "text-sm text-gray-500 hover:text-gray-900 transition-colors" %>
    </div>

  </div>
</div>
