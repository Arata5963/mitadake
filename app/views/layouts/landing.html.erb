<%#
  app/views/layouts/landing.html.erb
  ==========================================
  ランディングページ用レイアウト
  ==========================================

  【このファイルの役割】
  未ログインユーザー向けのランディングページ用レイアウト。
  ヘッダーやサイドバーを含まないシンプルな構成。

  【application.html.erb との違い】
  - ヘッダーなし（全画面表示）
  - フッターなし
  - 背景色が白（bg-white）
  - Flatpickr/Chart.jsなし（不要なため）

  【使用されるページ】
  pages/landing.html.erb（トップページ）
%>
<!DOCTYPE html>
<html lang="ja">

  <head>
    <%= display_meta_tags(default_meta_tags) %>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= yield :head %>
    <%# Tailwind を先に、Application を後に %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>

    <%= javascript_importmap_tags %>

    <!-- Google tag (gtag.js) -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-109R2FXJBS"></script>
    <script>
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('js', new Date());
      gtag('config', 'G-109R2FXJBS');
    </script>
  </head>

  <body class="min-h-screen bg-white">
    <%= yield %>
  </body>
</html>
