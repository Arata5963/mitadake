<%# app/views/users/designs/_mypage_vertical.html.erb %>
<%# デザインA: 縦型カード（オリジナル） %>

<% current_action = user.current_action_plan %>
<% current_video = user.current_video %>

<div class="max-w-sm mx-auto px-4 py-8">
  <%# プロフィールヘッダー（中央揃え） %>
  <div class="text-center mb-8">
    <div class="mb-3">
      <% if user.avatar.present? %>
        <%= image_tag user.avatar.url, class: "w-20 h-20 rounded-full object-cover mx-auto" %>
      <% else %>
        <div class="w-20 h-20 rounded-full bg-gray-100 flex items-center justify-center mx-auto">
          <svg class="w-10 h-10 text-gray-400" fill="currentColor" viewBox="0 0 20 20">
            <path fill-rule="evenodd" d="M10 9a3 3 0 100-6 3 3 0 000 6zm-7 9a7 7 0 1114 0H3z" clip-rule="evenodd" />
          </svg>
        </div>
      <% end %>
    </div>
    <div class="flex items-center justify-center gap-1 mb-1">
      <h1 class="text-xl font-bold text-gray-900"><%= user.name.presence || "名無しさん" %></h1>
      <%= link_to edit_profile_path, class: "p-1 text-gray-400 hover:text-gray-600" do %>
        <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
          <path stroke-linecap="round" stroke-linejoin="round" d="M15.232 5.232l3.536 3.536m-2.036-5.036a2.5 2.5 0 113.536 3.536L6.5 21.036H3v-3.572L16.732 3.732z" />
        </svg>
      <% end %>
    </div>
    <p class="text-sm text-gray-500"><%= user.post_entries.achieved.count %> アクション達成</p>
  </div>

  <% if current_video && current_action %>
    <%# メインカード %>
    <div class="bg-gray-50 rounded-2xl overflow-hidden" data-controller="hero-video" data-hero-video-video-id-value="<%= current_video.youtube_video_id %>">
      <%# 動画 %>
      <div class="group relative cursor-pointer" style="aspect-ratio: 16/9;">
        <iframe
          id="hero-player-<%= current_video.youtube_video_id %>"
          data-hero-video-target="iframe"
          src="https://www.youtube.com/embed/<%= current_video.youtube_video_id %>?autoplay=1&mute=1&loop=1&playlist=<%= current_video.youtube_video_id %>&controls=0&playsinline=1&rel=0&modestbranding=1&showinfo=0&enablejsapi=1"
          class="absolute inset-0 w-full h-full pointer-events-none"
          frameborder="0"
          allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
          allowfullscreen>
        </iframe>
        <div class="absolute inset-0 z-10 flex items-center justify-center opacity-0 group-hover:opacity-100 transition-opacity duration-300"
             data-action="click->hero-video#toggle">
          <div class="absolute inset-0 bg-black/30"></div>
          <button type="button" class="relative z-10 w-14 h-14 rounded-full bg-white/90 hover:bg-white flex items-center justify-center transition-transform hover:scale-110 shadow-lg">
            <svg data-hero-video-target="playButton" class="w-6 h-6 text-black ml-1 hidden" viewBox="0 0 24 24" fill="currentColor">
              <path d="M8 5v14l11-7z"/>
            </svg>
            <svg data-hero-video-target="pauseButton" class="w-6 h-6 text-black" viewBox="0 0 24 24" fill="currentColor">
              <path d="M6 19h4V5H6v14zm8-14v14h4V5h-4z"/>
            </svg>
          </button>
        </div>
      </div>

      <%# コンテンツ %>
      <div class="p-4">
        <%= link_to post_path(current_video), class: "block group/info mb-4" do %>
          <h3 class="text-base font-bold text-gray-900 line-clamp-2 group-hover/info:text-gray-600 transition-colors leading-snug">
            <%= current_video.youtube_title %>
          </h3>
          <% if current_video.youtube_channel_name.present? %>
            <div class="flex items-center gap-2 mt-2">
              <% if current_video.youtube_channel_thumbnail_url.present? %>
                <%= image_tag current_video.youtube_channel_thumbnail_url, class: "w-5 h-5 rounded-full", alt: current_video.youtube_channel_name %>
              <% end %>
              <span class="text-gray-500 text-sm"><%= current_video.youtube_channel_name %></span>
            </div>
          <% end %>
        <% end %>

        <div class="border-t border-gray-200 pt-4">
          <div class="flex items-start gap-3">
            <%= button_to achieve_post_post_entry_path(current_video, current_action),
                  method: :patch, class: "flex-shrink-0 mt-0.5",
                  form: { data: { turbo_confirm: "達成しますか？", turbo_frame: "_top" } } do %>
              <div class="w-6 h-6 rounded-full border-2 border-gray-900 cursor-pointer hover:bg-gray-200 transition-colors"></div>
            <% end %>
            <div class="flex-1 min-w-0">
              <p class="text-base text-gray-900 leading-relaxed"><%= current_action.content %></p>
              <% if current_action.deadline.present? %>
                <% is_overdue = current_action.deadline < Date.current %>
                <% is_today = current_action.deadline == Date.current %>
                <p class="text-sm mt-1 <%= is_overdue ? 'text-red-500' : (is_today ? 'text-gray-900 font-medium' : 'text-gray-500') %>">
                  <%= is_overdue ? '期限切れ' : (is_today ? '今日まで' : "#{l(current_action.deadline, format: :short)}まで") %>
                </p>
              <% end %>
            </div>
            <%= button_to post_post_entry_path(current_video, current_action),
                  method: :delete,
                  form: { data: { turbo_confirm: "削除しますか？", turbo_frame: "_top" } },
                  class: "flex-shrink-0 p-1 text-gray-300 hover:text-red-500 transition-colors" do %>
              <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5">
                <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12" />
              </svg>
            <% end %>
          </div>
        </div>
      </div>
    </div>
  <% else %>
    <%# Empty State %>
    <div class="bg-gray-50 rounded-2xl py-16 px-6 text-center">
      <div class="text-5xl mb-4">🎯</div>
      <h2 class="text-lg font-bold text-gray-900 mb-2">アクションプランがありません</h2>
      <p class="text-sm text-gray-500 mb-6">動画を見て、やることを決めましょう</p>
      <%= link_to posts_path, class: "inline-flex items-center gap-2 px-5 py-2.5 bg-gray-900 text-white text-sm rounded-full font-medium hover:bg-gray-700 transition-colors" do %>
        動画を探す
      <% end %>
    </div>
  <% end %>
</div>
