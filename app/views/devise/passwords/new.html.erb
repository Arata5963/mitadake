<%#
  app/views/devise/passwords/new.html.erb
  ==========================================
  パスワードリセット申請ページ
  ==========================================

  【このページの役割】
  パスワードを忘れたユーザーがリセットメールを受け取るためのページ。
  メールアドレスを入力するとリセット用リンクが送信される。

  【アクセス方法】
  ログインページの「パスワードをお忘れの方」リンクから

  【フォーム送信先】
  POST /users/password → Devise::PasswordsController#create

  【処理の流れ】
  1. メールアドレス入力 → 送信
  2. サーバーでユーザーを検索
  3. 存在すればリセットトークン生成 → メール送信
  4. 存在しなくても同じ画面表示（セキュリティ対策）

  【セキュリティ考慮】
  メールアドレスの存在有無を知られないよう、
  成功時も失敗時も同じメッセージを表示する。
%>
<% content_for :title, "パスワードをお忘れの方" %>

<div class="min-h-screen bg-white">
  <div class="max-w-sm mx-auto px-4 py-16">

    <h1 class="text-xl font-bold text-gray-900 mb-2">パスワードをお忘れの方</h1>
    <p class="text-sm text-gray-500 mb-8">
      登録されているメールアドレスを入力してください。<br>
      パスワードリセット用のリンクをお送りします。
    </p>

    <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post, class: "space-y-6", data: { turbo: false } }) do |f| %>
      <%= render "devise/shared/error_messages", resource: resource %>

      <div>
        <%= f.label :email, "メールアドレス", class: "block text-xs text-gray-500 mb-1" %>
        <%= f.email_field :email,
            autofocus: true,
            autocomplete: "email",
            placeholder: "mail@example.com",
            class: "w-full px-0 py-2 border-0 border-b border-gray-300 text-gray-900 placeholder-gray-300 focus:border-gray-900 focus:ring-0 focus:outline-none transition-colors bg-transparent" %>
      </div>

      <div class="pt-4">
        <%= f.submit "リセットメールを送信",
            class: "w-full bg-gray-900 text-white py-3 text-sm font-medium hover:bg-gray-800 transition-colors cursor-pointer" %>
      </div>
    <% end %>

    <div class="mt-8 pt-8 border-t border-gray-100">
      <%= link_to "← ログイン画面に戻る", new_user_session_path,
            class: "text-sm text-gray-500 hover:text-gray-900 transition-colors" %>
    </div>

  </div>
</div>
