<%#
  app/views/posts/recent.html.erb
  ==========================================
  最近の投稿一覧ページ
  ==========================================

  【このページの役割】
  最近登録された動画を一覧表示するページ。
  投稿一覧から「最近の投稿」リンクでアクセス。

  【アクセスURL】
  GET /posts/recent

  【表示内容】
  - ヘッダー（戻るリンク付き）
  - 動画カードのグリッド表示
  - ページネーション（Kaminari）

  【グリッドレイアウト】
  auto-fill + minmax で、画面幅に応じてカラム数が
  自動調整される。最小170px、最大1fr。

  【変数】
  @posts: ページネーション済みの最近の投稿
%>

<div class="min-h-screen bg-white">
  <div class="max-w-6xl mx-auto px-4 sm:px-8 py-6">
    <%# ヘッダー %>
    <h1 class="text-xl font-bold text-gray-900 mb-6">最近の投稿</h1>

    <% if @posts.blank? %>
      <div class="py-16 text-center">
        <span class="text-5xl block mb-4">📺</span>
        <p class="text-gray-700 font-medium mb-2">まだ投稿がありません</p>
      </div>
    <% else %>
      <%# グリッド表示 %>
      <div class="grid gap-4" style="grid-template-columns: repeat(auto-fill, minmax(170px, 1fr));">
        <% @posts.each do |post| %>
          <%= render 'posts/post_card_note', post: post %>
        <% end %>
      </div>

      <%# ページネーション %>
      <% if @posts.total_pages > 1 %>
        <div class="mt-8 flex justify-center">
          <%= paginate @posts %>
        </div>
      <% end %>
    <% end %>
  </div>
</div>
