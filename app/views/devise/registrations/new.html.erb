<%#
  app/views/devise/registrations/new.html.erb
  ==========================================
  新規登録ページ
  ==========================================

  【このページの役割】
  新規ユーザーがアカウントを作成するフォーム。

  【登録方法】
  1. メール + パスワード（Devise標準）
  2. Googleで登録（OAuth2）

  【フォーム要素】
  - ユーザー名入力（必須）
  - メールアドレス入力
  - パスワード入力（最小6文字、ヒント表示）
  - パスワード確認入力
  - ログインリンク

  【バリデーション表示】
  devise/shared/_error_messages でエラーを表示。

  【Turbo無効化】
  data: { turbo: false } でTurbo Driveを無効化。
  Deviseの登録処理との互換性のため。

  【コントローラー】
  Users::RegistrationsController#new, #create
%>
<% content_for :title, "新規登録" %>

<div class="min-h-screen bg-white">
  <div class="max-w-sm mx-auto px-4 py-16">

    <h1 class="text-xl font-bold text-gray-900 mb-8">新規登録</h1>

    <%= form_for(resource, as: resource_name, url: registration_path(resource_name),
          html: { class: "space-y-6", data: { turbo: false } }) do |f| %>

      <%= render "devise/shared/error_messages", resource: resource %>

      <div>
        <%= f.label :name, "ユーザー名", class: "block text-xs text-gray-500 mb-1" %>
        <%= f.text_field :name, autofocus: true, autocomplete: "name",
              placeholder: "ユーザー名を入力",
              required: true,
              class: "w-full px-0 py-2 border-0 border-b border-gray-300 text-gray-900 placeholder-gray-300 focus:border-gray-900 focus:ring-0 focus:outline-none transition-colors bg-transparent autofill:bg-transparent autofill:shadow-[inset_0_0_0px_1000px_rgb(255,255,255)]" %>
      </div>

      <div>
        <%= f.label :email, "メールアドレス", class: "block text-xs text-gray-500 mb-1" %>
        <%= f.email_field :email, autocomplete: "email",
              placeholder: "mail@example.com",
              class: "w-full px-0 py-2 border-0 border-b border-gray-300 text-gray-900 placeholder-gray-300 focus:border-gray-900 focus:ring-0 focus:outline-none transition-colors bg-transparent autofill:bg-transparent autofill:shadow-[inset_0_0_0px_1000px_rgb(255,255,255)]" %>
      </div>

      <%= render 'shared/password_field',
            form: f,
            field_name: :password,
            label_text: "パスワード",
            placeholder: "パスワードを入力",
            autocomplete: "new-password",
            show_hint: true,
            minimum_length: @minimum_password_length %>

      <%= render 'shared/password_field',
            form: f,
            field_name: :password_confirmation,
            label_text: "パスワード（確認用）",
            placeholder: "パスワードを再入力",
            autocomplete: "new-password" %>

      <div class="pt-4">
        <%= f.submit "登録する",
              class: "w-full bg-gray-900 text-white py-3 text-sm font-medium hover:bg-gray-800 transition-colors cursor-pointer" %>
      </div>
    <% end %>

    <div class="mt-8 pt-8 border-t border-gray-100">
      <%= button_to user_google_oauth2_omniauth_authorize_path,
                    method: :post,
                    data: { turbo: false },
                    class: "w-full flex items-center justify-center gap-3 py-3 border border-gray-200 text-gray-700 text-sm font-medium hover:bg-gray-50 transition-colors" do %>
        <svg class="h-4 w-4" viewBox="0 0 24 24">
          <path fill="#4285F4" d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92c-.26 1.37-1.04 2.53-2.21 3.31v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.09z"/>
          <path fill="#34A853" d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z"/>
          <path fill="#FBBC05" d="M5.84 14.09c-.22-.66-.35-1.36-.35-2.09s.13-1.43.35-2.09V7.07H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.93l2.85-2.22.81-.62z"/>
          <path fill="#EA4335" d="M12 5.38c1.62 0 3.06.56 4.21 1.64l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.07l3.66 2.84c.87-2.6 3.3-4.53 6.16-4.53z"/>
        </svg>
        <span>Googleで登録</span>
      <% end %>
    </div>

    <p class="mt-8 text-sm text-gray-500 text-center">
      アカウントをお持ちの方は
      <%= link_to "ログイン", new_session_path(resource_name),
            class: "text-gray-900 font-medium hover:underline" %>
    </p>

  </div>
</div>
